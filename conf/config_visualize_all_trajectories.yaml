# Description: Configuration file for the visualizer - see README.md for more details

# Name of the results file to visualize
results: "unfiltered_trajectories_.csv"
raw_image: "Large mvt 4.nd2"

# Name of the experiment to visualize
experiment_name: "medium_mvt_6000"

# Path Settings (relative to project root)
project_path: "."
data_path: "data"

# Data directories (relative to data_path)
raw_dir: "01_raw"
results_dir: "05_results"

# Frames range to consider
frame_range: [1,2] # [first_frame, last_frame] - If null, all frames are considered. 
